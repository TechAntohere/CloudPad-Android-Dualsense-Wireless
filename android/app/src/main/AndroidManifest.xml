<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- DualSense wireless (BT HID) feedback stack -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:allowBackup="true"
        android:banner="@drawable/tv_banner"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <provider
            android:authorities="${applicationId}.fileprovider"
            android:name="androidx.core.content.FileProvider"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths"
                />
        </provider>

        <activity android:name="com.metallic.chiaki.main.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.pylux.stream.action.LAUNCH_CLOUD_GAME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.metallic.chiaki.stream.StreamActivity"
            android:exported="false"
            android:theme="@style/StreamTheme"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize"
            android:screenOrientation="userLandscape"
            android:supportsPictureInPicture="true"/>

        <activity
            android:name="com.metallic.chiaki.settings.SettingsActivity"
            android:exported="false" />

        <activity
            android:name="com.metallic.chiaki.trophy.TrophiesActivity"
            android:exported="false" />

        <activity
            android:name="com.metallic.chiaki.friends.FriendsActivity"
            android:exported="false" />

        <activity
            android:name="com.metallic.chiaki.friends.FriendChatActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name="com.metallic.chiaki.friends.TrophyCompareActivity"
            android:exported="false" />

        <activity
            android:name="com.metallic.chiaki.settings.ControllerRemapActivity"
            android:exported="false"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize" />

        <activity
            android:name="com.metallic.chiaki.regist.RegistActivity"
            android:exported="false"
            android:theme="@style/MageTheme"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize"
            android:windowSoftInputMode="adjustResize"/>

        <activity
            android:name="com.metallic.chiaki.regist.RegistExecuteActivity"
            android:exported="false"
            android:theme="@style/MageTheme"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize" />

        <activity
            android:name="com.metallic.chiaki.manualconsole.EditManualConsoleActivity"
            android:exported="false"
            android:theme="@style/MageTheme"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize" />

        <activity
            android:name="com.metallic.chiaki.cloudplay.PsnLoginActivity"
            android:exported="false"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize" />

        <activity
            android:name="com.metallic.chiaki.common.LicenseAgreementActivity"
            android:exported="false"
            android:theme="@style/LicenseAgreementDialog"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize"
            android:launchMode="singleTop" />

        <!-- DualSense wireless: captures system audio for the controller
             speaker / headphone-jack output path. -->
        <service
            android:name="com.metallic.chiaki.stream.ControllerSystemAudioService"
            android:foregroundServiceType="mediaProjection"
            android:exported="false" />

        <!-- DualSense wireless: keeps the BT HID output-report bridge alive
             while streaming. -->
        <service
            android:name="com.metallic.chiaki.stream.ControllerBtForegroundService"
            android:foregroundServiceType="connectedDevice"
            android:exported="false" />
    </application>

</manifest>