<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/mainStreamLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.metallic.chiaki.stream.StreamActivity"
    android:keepScreenOn="true">

    <com.metallic.chiaki.stream.AspectRatioFrameLayout
        android:id="@+id/aspectRatioLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center">
        <com.metallic.chiaki.stream.CasVideoSurfaceView
            android:id="@+id/surfaceView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>
    </com.metallic.chiaki.stream.AspectRatioFrameLayout>

    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"/>

    <com.metallic.chiaki.stream.PerformanceOverlayView
        android:id="@+id/performanceOverlay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_margin="8dp"
        android:visibility="gone"/>

    <!-- Trophy unlock popup: shown by TrophyUnlockPopupPresenter for up to 10s per trophy.
         Positioned below the (usually-off) Performance Overlay to avoid the two overlapping. -->
    <LinearLayout
        android:id="@+id/trophyUnlockPopup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_marginStart="8dp"
        android:layout_marginTop="88dp"
        android:orientation="vertical"
        android:paddingHorizontal="12dp"
        android:paddingVertical="10dp"
        android:background="@drawable/bg_trophy_unlock_popup"
        android:visibility="gone"
        android:elevation="8dp">

        <TextView
            android:id="@+id/trophyUnlockPopupHeader"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="6dp"
            android:text="@string/trophy_unlock_popup_header"
            android:textColor="?attr/pyluxAccent"
            android:textSize="12sp"
            android:textStyle="bold"/>

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <ImageView
                android:id="@+id/trophyUnlockPopupIcon"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:scaleType="centerCrop"/>

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="10dp"
                android:layout_marginEnd="10dp"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/trophyUnlockPopupText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:maxWidth="200dp"
                    android:textColor="@android:color/white"
                    android:textSize="14sp"
                    android:textStyle="bold"/>

                <TextView
                    android:id="@+id/trophyUnlockPopupDetail"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:maxWidth="200dp"
                    android:maxLines="2"
                    android:ellipsize="end"
                    android:textColor="#B3B3B3"
                    android:textSize="12sp"/>
            </LinearLayout>

            <TextView
                android:id="@+id/trophyUnlockPopupBadge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:paddingTop="3dp"
                android:paddingBottom="3dp"
                android:textColor="#000000"
                android:textSize="9sp"
                android:textStyle="bold"/>
        </LinearLayout>
    </LinearLayout>

    <fragment
        android:id="@+id/controlsFragment"
        android:name="com.metallic.chiaki.touchcontrols.DefaultTouchControlsFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <fragment
        android:id="@+id/touchpadOnlyFragment"
        android:name="com.metallic.chiaki.touchcontrols.TouchpadOnlyFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <!-- Quick Settings is its own Dialog window now (see QuickSettingsPanel.kt), not an
         in-window View — sharing a window with the continuously-updating video SurfaceView
         proved unreliable to animate/composite correctly even with visibility and hardware-
         layer workarounds. A separate window is composited above the activity deterministically. -->

    <!-- DualSense headphone volume HUD. Shown by StreamActivity only while the
         controller's 3.5mm jack is active, when the hardware volume keys are
         retargeted from phone volume to the controller's headphone output.
         Non-interactive: it is feedback for the key presses, not a control. -->
    <FrameLayout
        android:id="@+id/headphoneVolumeOverlay"
        android:layout_width="68dp"
        android:layout_height="220dp"
        android:layout_gravity="center_vertical|end"
        android:layout_marginEnd="24dp"
        android:alpha="0"
        android:background="@drawable/bg_trophy_unlock_popup"
        android:clickable="false"
        android:focusable="false"
        android:padding="10dp"
        android:visibility="gone"
        tools:alpha="1"
        tools:visibility="visible">

        <SeekBar
            android:id="@+id/headphoneVolumeSeekBar"
            android:layout_width="180dp"
            android:layout_height="48dp"
            android:layout_gravity="center"
            android:max="100"
            android:progress="100"
            android:progressTint="?attr/pyluxAccent"
            android:progressBackgroundTint="@android:color/white"
            android:thumbTint="?attr/pyluxAccent"
            android:rotation="-90" />

        <TextView
            android:id="@+id/headphoneVolumeTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|center_horizontal"
            android:gravity="center"
            android:textColor="@android:color/white"
            android:textSize="12sp"
            android:textStyle="bold"
            tools:text="80%" />
    </FrameLayout>

</FrameLayout>