<?xml version="1.0" encoding="utf-8"?>

<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="12dp"
    android:layout_marginStart="16dp"
    android:layout_marginEnd="16dp"
    android:layout_marginTop="12dp"
    android:layout_marginBottom="4dp"
    app:cardElevation="6dp"
    app:strokeWidth="1dp"
    app:strokeColor="?attr/pyluxAccentA20"
    android:focusable="true"
    android:clickable="true"
    android:foreground="?android:attr/selectableItemBackground">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="180dp">

        <!-- Header Background with Gradient Effect -->
        <View
            android:id="@+id/headerBackground"
            android:layout_width="0dp"
            android:layout_height="60dp"
            android:background="@drawable/console_card_header_gradient"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Console Name in Header -->
        <TextView
            android:id="@+id/headerNameTextView"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:textSize="22sp"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:letterSpacing="0.01"
            android:maxLines="1"
            android:ellipsize="end"
            android:layout_marginStart="20dp"
            android:layout_marginEnd="112dp"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="16dp"
            app:layout_constraintTop_toTopOf="@id/headerBackground"
            app:layout_constraintBottom_toBottomOf="@id/headerBackground"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:text="PS5-289" />

        <!-- Platform Badge (PS4 or PS5 — Remote Play discovery never surfaces a PS3, that's
             Cloud Play only, on a different screen) -->
        <TextView
            android:id="@+id/platformBadge"
            android:layout_width="wrap_content"
            android:layout_height="32dp"
            android:minWidth="32dp"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:background="@drawable/platform_badge_background"
            android:gravity="center"
            android:fontFamily="sans-serif-black"
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:layout_marginEnd="8dp"
            app:layout_constraintTop_toTopOf="@id/headerBackground"
            app:layout_constraintBottom_toBottomOf="@id/headerBackground"
            app:layout_constraintEnd_toStartOf="@id/menuButton"
            tools:text="PS5" />

        <!-- Hidden connection type indicator (kept for binding reference) -->
        <ImageView
            android:id="@+id/discoveredIndicatorLayout"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:visibility="gone"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Menu Button -->
        <androidx.appcompat.widget.AppCompatImageButton
            android:id="@+id/menuButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_overflow"
            android:padding="8dp"
            android:layout_marginEnd="8dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            app:layout_constraintTop_toTopOf="@id/headerBackground"
            app:layout_constraintBottom_toBottomOf="@id/headerBackground"
            app:layout_constraintEnd_toEndOf="parent"
            app:tint="@android:color/white" />

        <!-- Hidden console icon for state detection -->
        <ImageView
            android:id="@+id/stateIndicatorImageView"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:src="@drawable/ic_console_ps5"
            android:visibility="gone"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Content Container -->
        <LinearLayout
            android:id="@+id/contentContainer"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="20dp"
            android:paddingEnd="20dp"
            android:paddingTop="14dp"
            android:paddingBottom="14dp"
            app:layout_constraintTop_toBottomOf="@id/headerBackground"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent">

            <!-- Hidden duplicate name for binding -->
            <TextView
                android:id="@+id/nameTextView"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:visibility="gone"
                tools:text="PS5-289" />

            <!-- Hidden platform text for binding compatibility -->
            <TextView
                android:id="@+id/platformTextView"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:visibility="gone"
                tools:ignore="UnusedAttribute"
                tools:text="PS5" />

            <!-- Info Row: Connection on left, Status on right -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="0dp">

                <!-- Left side: Connection Info -->
                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical">

                    <!-- Connection Info -->
                    <TextView
                        android:id="@+id/hostTextView"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:fontFamily="sans-serif-medium"
                        android:textSize="15sp"
                        android:textColor="#FFFFFF"
                        android:maxLines="1"
                        android:ellipsize="end"
                        tools:text="Address: 192.168.12.114" />

                    <!-- Device ID -->
                    <TextView
                        android:id="@+id/idTextView"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="4dp"
                        android:fontFamily="sans-serif"
                        android:textSize="13sp"
                        android:textColor="#CCCCCC"
                        android:maxLines="1"
                        android:ellipsize="middle"
                        tools:text="MAC: 28:40:DD:50:40:3A" />

                </LinearLayout>

                <!-- Right side: Status with colored dot -->
                <LinearLayout
                    android:id="@+id/statusLayout"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:layout_marginStart="16dp"
                    android:layout_gravity="center_vertical"
                    android:gravity="center_vertical"
                    android:visibility="gone"
                    tools:visibility="visible">

                    <ImageView
                        android:id="@+id/statusIcon"
                        android:layout_width="12dp"
                        android:layout_height="12dp"
                        android:src="@drawable/ic_circle"
                        android:layout_marginEnd="6dp"
                        app:tint="?attr/colorPrimary" />

                    <TextView
                        android:id="@+id/statusTextView"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:fontFamily="sans-serif"
                        android:textSize="14sp"
                        android:textColor="#808080"
                        android:maxLines="2"
                        android:gravity="end"
                        tools:text="Ready" />

                </LinearLayout>

            </LinearLayout>

            <!-- Bottom Info (App/Game running) -->
            <TextView
                android:id="@+id/bottomInfoTextView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:paddingTop="12dp"
                android:background="@drawable/bottom_info_background"
                android:paddingStart="12dp"
                android:paddingEnd="12dp"
                android:paddingBottom="12dp"
                android:fontFamily="sans-serif-medium"
                android:textSize="13sp"
                android:textColor="?attr/colorOnSurface"
                android:gravity="start"
                android:maxLines="2"
                android:ellipsize="end"
                android:visibility="gone"
                tools:visibility="visible"
                tools:text="App: Persona 5 Royal\nTitle ID: CUSA12345" />

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>